# Netlify-style SPA fallback - sends all non-asset paths to index.html
# so React Router can handle the route on the client.
# (Replaces what HashRouter did natively. Without this, /dakiraty refreshes 404.)
/*    /index.html   200
